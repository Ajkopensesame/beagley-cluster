[Unit]
# systemd-resolved uses PrivateTmp, which touches /var/tmp before the resolver
# is spawned. On this appliance /var/tmp points into /var/volatile, so make the
# volatile tmpfiles pass an explicit prerequisite before DNS starts.
Wants=systemd-tmpfiles-setup.service
After=systemd-tmpfiles-setup.service
