[Unit]
# Keep time sync behind the same volatile directory setup used by resolved.
# Without this, HTTPS map/radar fetches can fail after a cold boot because the
# board clock stays in the past until NTP starts.
Wants=systemd-tmpfiles-setup.service
After=systemd-tmpfiles-setup.service
